# 环境监测保护系统 · 开发流程提示词（分段式）

> 生成日期：2025-08-25 · 工作区：`C:\Users\LZ\Desktop\drenepsystem\`
> 用途：本文件是整个项目的**总指挥文档**。每一段包含：段目标 → 可复用 GitHub 开源项目 → 可直接复制给 AI 的提示词 → 验收标准。**逐段执行，每段验收通过后再进入下一段。**

---

## 0. 项目概况（已确认）

| 项目 | 内容 |
|---|---|
| 系统定位 | 多合一通用环境监测平台（空气/水质/噪声等），课程设计/毕业设计定位 |
| 技术栈 | 后端 Spring Boot 2.6.13 + MyBatis-Plus 3.4.1 + MySQL；前端 Vue3 + Vite + Element-Plus + Axios + ECharts |
| 数据来源 | **模拟数据优先**（模拟器生成定时数据），**预留 MQTT 硬件接口**（后期接 ESP32/Arduino） |
| 现有资产 | `nepsystem/` Spring Boot 骨架（admins 表 CRUD 已通）；本机 JDK21/Maven3.9.9/Node22/MySQL80 就绪 |
| 已知问题 | ① MySQL root 密码未知（`application.properties` 里是 `mysql`，实测失败）；② pom.xml 中 spring-boot-maven-plugin 有 `<skip>true</skip>` 需删除；③ 数据库 `nep` 的 8 张表未建全 |

### 目标系统架构（文字版）

```
┌─ 数据源层 ────────────────────────────────┐
│ 硬件传感器(预留): ESP32/Arduino → MQTT     │
│ 模拟器: Simulator 定时生成随机监测数据       │
└──────────────┬────────────────────────────┘
               ▼
┌─ 后端层 Spring Boot ──────────────────────┐
│ 设备管理 | 数据接收Ingest | 告警引擎(阈值)  │
│ 统计聚合 | 用户/权限 | WebSocket 实时推送   │
└──────────────┬────────────────────────────┘
               ▼
┌─ 数据层 MySQL(nep) ───────────────────────┐
│ admins users devices sensors monitor_data │
│ thresholds alerts regions                  │
└───────────────────────────────────────────┘
               ▼
┌─ 前端层 Vue3 ─────────────────────────────┐
│ 登录/布局 | 实时监测大屏 | 历史曲线 |       │
│ 设备管理 | 告警中心 | 用户管理             │
└───────────────────────────────────────────┘
```

---

## 1. GitHub 开源项目速查表（可复用性排序）

> 使用方式：在对应阶段开发前，先 clone 或打开该仓库的 GitHub 页面，把"可复用点"对应的代码文件结构读一遍，再让 AI 参考其设计实现本系统。**复用的是设计与结构，不是直接抄代码**（避免许可证与依赖冲突）。

| # | 项目 | 链接 | 技术栈 | 可复用点 | 对应阶段 |
|---|---|---|---|---|---|
| 1 | **smart-env-monitor** 智能环境监测平台 | https://github.com/luoming-lot/smart-env-monitor | ESP32 + MQTT + Spring Boot + React + MySQL | 全栈 IoT 分层结构、设备/数据表设计、MQTT 接入、后端 Controller-Service-DAO 分层 | 2/3/4 |
| 2 | **airpulse** 空气质量实时监测 | https://github.com/djlord-it/airpulse | Kafka + Spring Boot + PostgreSQL + Redis + Prometheus | **AQI 分级计算算法**、阈值告警触发机制、统计/仪表盘 API 设计、模拟传感器数据源 | 3/4/6 |
| 3 | **Environmental-Public-Supervision-System** 环保公众监督系统 | https://github.com/Ljj-user/Environmental-Public-Supervision-System | Spring Boot + 前端 | 环境数据上报流程、监督/举报业务模型、后台管理页面结构 | 3/6 |
| 4 | **Opikadash/Environment-monitoring-system** | https://github.com/Opikadash/Environment-monitoring-system | MQTT + Node-RED + InfluxDB + Grafana | **纯模拟传感器方案**（无需硬件跑通全链路）、实时趋势可视化、告警规则思路 | 4/6 |
| 5 | **CJL-build/-** 物联网环境监测（含浇水控制） | https://github.com/CJL-build/- | Vue + Spring Boot + 硬件 | 前后端分离完整结构、**阈值到达自动动作**（告警/联动）、Vue 实时数据显示 | 3/5/6 |
| 6 | **IoT-Air-Quality-Pollution-Monitoring-Dashboard** | https://github.com/varda24/IoT-Air-Quality-Pollution-Monitoring-Dashboard | ESP32 + 云仪表盘 | AQI 分类（优/良/轻度/中度/重度/严重）分级逻辑、实时告警 | 3 |
| 7 | **MatoYing/GraduationProject** 基础平台监控系统（毕设） | https://github.com/MatoYing/GraduationProject | SpringBoot + Vue 前后端分离 | **毕业设计项目组织方式**：文档/目录/接口风格，可直接模仿其 README 与演示组织 | 全部 |
| 8 | **gubbriaco/weather-station** | https://github.com/gubbriaco/weather-station | DHT11 + Spring Boot | 传感器采集 → Spring Boot 接口（POST/GET）的最小实现参考 | 4 |
| 9 | **EnviroSnoop** | https://github.com/ageagainstthemachine/EnviroSnoop | Pico W + InfluxDB | 采集端与存储端分离思路、数据上报协议设计 | 4 |
| 10 | **Air_Quality_Monitoring_System** | https://github.com/aroyy007/Air_Quality_Monitoring_System | Arduino + Node.js + React | 健康告警 + 历史数据分析的 UI 交互模式 | 6 |

> 💡 若 GitHub 打不开，可让 AI 用 `web_search` 检索对应项目的 README/代码片段摘要，照样能提取设计思路。

---

## 2. 分段式开发流程（共 8 段）

### 全局提示词（每段任务前先粘贴）

```
你是资深全栈工程师（Java Spring Boot + Vue3）。请遵循以下规则：
1. 代码规范：Java 用 4 空格缩进、类名大驼峰、方法小驼峰；遵循阿里 Java 开发手册基本约定；
2. 分层规范：ctrl(Controller) → service(业务) → dao(MyBatis-Plus) → bean(实体)，禁止在 Controller 写业务逻辑；
3. 所有接口统一返回 JSON 格式：{code, message, data}，code=200 成功、500 失败；
4. 涉及数据库变更时，同步更新数据库初始化 SQL 脚本（放在 nepsystem/src/main/resources/db/ 下）；
5. 每完成一个功能点，给出验证方式（curl 命令或浏览器访问路径）；
6. 中文注释，注释说明业务意图而非代码翻译；
7. 修改文件前先读原文件，保持与现有代码风格一致；
8. 遇到报错时：先贴出完整报错堆栈，再分析原因，给出修复方案后执行。
```

---

### 段 1：基础设施修复与数据库设计（约 1 小时）

**段目标**：修复项目无法连库/无法打包的问题；设计并创建全部数据表；打通"启动 → 连库 → 接口"链路。

**前置条件**：确认 MySQL root 真实密码（见"已知问题"①）。

**参考项目**：smart-env-monitor（表设计）、airpulse（指标字典与告警表设计）

**AI 提示词**：

```
我在 C:\Users\LZ\Desktop\drenepsystem\nepsystem 有一个 Spring Boot 2.6.13 + MyBatis-Plus 项目（nep 数据库）。请帮我完成以下任务：

【任务A】修复构建问题
1. 删除 pom.xml 中 spring-boot-maven-plugin 的 <skip>true</skip> 配置；
2. 检查 application.properties 的数据库配置（root 密码我稍后确认后填入）；
3. 执行 mvn compile 确认 BUILD SUCCESS。

【任务B】设计并生成数据库初始化脚本（保存到 src/main/resources/db/init.sql）
为"多合一环境监测保护平台"设计 MySQL 表（库名 nep，utf8mb4，InnoDB）：
1. admins 管理员表（已有，保持 admin_id 主键）
2. users 用户表（id, username, password, nickname, role, status, create_time）
3. regions 区域表（id, name, parent_id, description）—— 监测点位归属区域
4. devices 监测设备/站点表（id, device_code 唯一, device_name, type 空气/水质/噪声, region_id, location, status 在线/离线/停用, last_report_time, create_time）
5. sensors 指标字典表（id, sensor_code 唯一如 PM25/CO2/TEMP/HUMI/PH/TURBIDITY/NOISE, sensor_name, unit, device_type 适用设备类型, min_range, max_range, standard_max 标准限值）
6. monitor_data 监测数据表（id, device_id, sensor_code, value DECIMAL(10,2), report_time, create_time，加索引 (device_id, report_time)）
7. thresholds 告警阈值表（id, device_id 可空=全局, sensor_code, warn_min, warn_max, alarm_min, alarm_max, enabled, update_time）—— 预警/报警两级
8. alerts 告警记录表（id, device_id, sensor_code, level 预警/报警, alert_value, message, status 未处理/已处理, handle_user, handle_time, create_time）

要求：所有表加 CREATE TABLE IF NOT EXISTS；monitor_data 与 alerts 是核心表，字段和索引要合理；每个表附 2-3 行中文注释；最后给出插入 3 台设备、5 个指标、2 条阈值的示例 INSERT。
```

**验收标准**：
- [ ] `mvn compile` 成功；`mvn spring-boot:run` 能真正启动（不再被 skip）
- [ ] init.sql 在 Navicat/命令行执行成功，8 张表全部创建
- [ ] 用真实密码更新 application.properties 后，`/admins/insert` 返回 success 且数据库出现记录
- [ ] 示例数据（3 设备 + 5 指标 + 2 阈值）已插入

---

### 段 2：后端数据模型与通用封装（约 1.5 小时）

**段目标**：为 8 张表生成实体/DAO/Service 骨架，统一响应与异常处理。

**参考项目**：smart-env-monitor（分层）、CJL-build（Service 层事务）

**AI 提示词**：

```
请基于 nepsystem 项目（org.nep.nepsystem 包）为数据库 8 张表（admins/users/regions/devices/sensors/monitor_data/thresholds/alerts）生成完整代码骨架，全部遵循 MyBatis-Plus 风格：

1. bean 包：每个表一个实体类，@TableName + @TableId + @TableField 驼峰映射，Lombok 注解（@Data），日期用 java.util.Date + @JsonFormat(pattern="yyyy-MM-dd HH:mm:ss")；
2. dao 包：每个实体一个接口继承 BaseMapper<T>，@Mapper 注解；
3. service 包：每个实体一个 Service 接口 + impl 实现类（继承 ServiceImpl<Dao, Entity>），提供基础的 CRUD 方法签名（可分页条件查询 QueryWrapper）；
4. 通用封装：
   - common.Result<T>：{code, message, data} 统一响应类 + 静态成功/失败方法；
   - common.PageResult<T>：分页结果（total, pages, records）；
   - common.GlobalExceptionHandler：@RestControllerAdvice 捕获业务异常与参数校验异常，返回统一格式；
   - exception.BizException：自定义业务异常；
5. 配置类 config.MybatisPlusConfig：分页插件 PaginationInnerInterceptor；
6. 不要写业务逻辑，只生成骨架；每个 Service 接口用中文注释说明用途。
```

**验收标准**：
- [ ] `mvn compile` 成功
- [ ] 统一返回格式已生效：访问任意不存在的 URL 返回 {code:500,...} 而非默认错误页
- [ ] 分页插件可用（写一个临时测试接口验证）

---

### 段 3：后端核心业务 API（约 3 小时）

**段目标**：实现设备管理、数据上报/查询、告警引擎、统计 API 全部接口。

**参考项目**：airpulse（AQI 计算+告警+统计）、Environmental-Public-Supervision-System（上报流程）、varda24（AQI 分级）

**AI 提示词**：

```
为 nepsystem 实现以下 REST API（统一前缀 /api，返回 Result 格式），Controller 只做参数接收，业务放 Service：

【设备管理 /api/devices】
- GET /api/devices/page?page=&size=&keyword=&type=&status= 分页查询（关键字匹配名称/编号，支持类型、状态过滤）
- POST /api/devices 新增（device_code 唯一校验）
- PUT /api/devices 修改
- DELETE /api/devices/{id} 删除（逻辑删除或校验无关联数据）
- GET /api/devices/online/count 在线设备数
- GET /api/devices/{id}/latest 设备最新一次各指标数据（Map<sensorCode, value>）

【监测数据 /api/data】
- POST /api/data/report 数据上报接口：body 为 {deviceCode, items:[{sensorCode, value}], reportTime}，做三件事：① 写入 monitor_data；② 更新设备 last_report_time 与在线状态；③ 触发阈值校验（见告警）；返回 Result；
- GET /api/data/latest?deviceId=&sensorCodes=a,b,c 最新数据
- GET /api/data/history?deviceId=&sensorCode=&start=&end=&page=&size= 历史分页（时间倒序）
- GET /api/data/trend?deviceId=&sensorCode=&start=&end=&interval=hour 趋势聚合（按小时/天求平均，SQL date_format 分组）

【告警 /api/alerts】
- 阈值校验逻辑：查询 thresholds 表（device 级优先于全局），值超过 alarm 区间记"报警"、超过 warn 区间记"预警"，写入 alerts（同一设备同指标同级别 30 分钟内不重复告警——查最近一条记录时间判断）
- GET /api/alerts/page?page=&size=&level=&status=&deviceId= 分页查询
- PUT /api/alerts/{id}/handle 处理告警（记录处理人/时间）
- GET /api/alerts/unhandled/count 未处理数
- GET /api/alerts/stat 近7天每日告警数（按 level 分组）

【阈值 /api/thresholds】
- GET/POST/PUT/DELETE 阈值 CRUD（deviceId 可空表示全局默认）

【统计 /api/stats】
- GET /api/stats/overview 总览：设备总数/在线数/今日上报条数/未处理告警数
- GET /api/stats/quality?deviceId=&start=&end= 环境质量评价：参照 AQI 思路，把每个指标值与 standard_max 对比打分 0-100（>100 为超标），返回各指标得分与综合得分
- GET /api/stats/device-ranking?start=&end= 设备上报量排行 Top10

要求：所有分页接口返回 PageResult；数据上报接口是核心，要处理事务（数据写入+设备更新+告警写入要么全成功要么全回滚）；每个接口实现后给出 curl 测试命令示例。
```

**验收标准**：
- [ ] 所有接口用 curl/Postman 调用成功
- [ ] 数据上报触发阈值 → alerts 表出现记录（且 30 分钟内不重复）
- [ ] 分页/趋势/统计接口返回结构正确
- [ ] 数据库事务正常（模拟中途异常不回滚脏数据）

---

### 段 4：模拟数据采集器 + MQTT 硬件接口预留（约 2 小时）

**段目标**：实现定时模拟器自动上报数据（空气/水质/噪声三种设备），并预留 MQTT 接入接口，让系统"自己会跑"。

**参考项目**：Opikadash（纯模拟方案）、airpulse（模拟数据源）、smart-env-monitor（MQTT 接入）、gubbriaco/weather-station（最小上报）

**AI 提示词**：

```
为 nepsystem 增加模拟数据源与 MQTT 预留，设计要点：

【模拟器 simulator】
1. 新建 config.SimulatorConfig 配置类：@ConfigurationProperties(prefix="simulator")，字段 enabled、intervalMs=5000（上报间隔）、deviceCodes 列表（从 devices 表读取全部启用设备）；
2. 新建 service.simulator.DataSimulatorService：@Scheduled(fixedDelayString="${simulator.intervalMs}") 定时任务（在启动类加 @EnableScheduling）；
3. 每个设备按 type 生成对应指标数据：
   - 空气: TEMP(18-35) HUMI(30-80) PM25(0-150) CO2(350-1200)
   - 水质: PH(6.5-9.0) TURBIDITY(0-20) DO 溶解氧(4-12) TEMP
   - 噪声: NOISE(30-95) 分贝
   用 Random 生成，注意 PM25 偶尔（5%概率）产生 150-300 的峰值用于演示告警；
4. 复用 /api/data/report 的 Service 方法上报（不直接调 HTTP，注入 Service 调用）；
5. 生成的数据条数记录日志（debug 级别，避免刷屏）。

【MQTT 预留 hardware】
6. 新建 mqtt.MqttProperties 配置类（enabled=false 默认关闭、broker=tcp://localhost:1883、topic=env/{deviceCode}/data、username/password）；
7. 新建 mqtt.MqttGateway 接口：@MessagingGateway(defaultRequestChannel="mqttOutboundChannel")，方法 publish(String topic, String payload)；
8. 新建 mqtt.MqttConfig：声明 MqttPahoClientFactory、@ServiceActivator 出站通道、MQTT 入站通道（topic=env/+/data）——入站消息解析 JSON 后调用同一个数据上报 Service；
9. 全部用 spring-integration-mqtt 依赖（版本随 Boot 2.6.13 管理），pom 中加依赖；enabled=false 时不创建 bean（用 @ConditionalOnProperty）；
10. 编写文档 README-MQTT.md：说明真实硬件如何对接（ESP32 发 JSON 到 topic，字段与 /api/data/report 一致）。
```

**验收标准**：
- [ ] 启动应用后每 5 秒自动产生数据，monitor_data 表持续增长
- [ ] 偶发 PM25 峰值能触发 alerts 表告警
- [ ] 设备 last_report_time 持续更新，状态为在线
- [ ] mqtt.enabled=false 时应用正常启动（无 MQTT broker 也不报错）
- [ ] README-MQTT.md 已生成

---

### 段 5：前端工程搭建与基础框架（约 2 小时）

**段目标**：创建 Vue3 + Vite 前端工程，完成登录、布局、请求封装、路由守卫。

**参考项目**：MatoYing/GraduationProject（前后端分离组织）、CJL-build（Vue 结构）

**AI 提示词**：

```
请在 C:\Users\LZ\Desktop\drenepsystem 下创建前端工程 nepsystem-web（用 Vite 脚手架 vue-ts 模板）：

1. 依赖：vue-router@4、pinia、axios、element-plus、@element-plus/icons-vue、echarts、sass；
2. 目录结构：src/api（按模块封装的请求）、src/router、src/store、src/views、src/components、src/utils/request.ts、src/layout；
3. utils/request.ts：axios 实例，baseURL=http://localhost:8080/api，请求拦截器带 token（Authorization: Bearer xxx），响应拦截器统一处理 {code,message,data}（code!=200 时 ElMessage 报错并 reject，401 跳登录）；
4. 页面：
   - views/Login.vue：管理员登录表单（调用 /api/auth/login，账号密码校验，成功后存 token 到 localStorage 并跳转 /dashboard）
   - layout/MainLayout.vue：侧边菜单（实时监测/历史数据/设备管理/告警中心/阈值设置/用户管理）+ 顶部栏（用户信息、退出）
5. 路由：/login 公开；其余在 layout 下，路由守卫 beforeEach 检查 token，无 token 跳登录；
6. vite.config.ts 配 devServer.proxy：/api → http://localhost:8080（避免跨域）；
7. 后端需配合新增 POST /api/auth/login（查 admins 表，密码比对，返回 token——先返回 UUID 即可，不用 JWT 框架）、POST /api/auth/logout；
8. 完成后给出 npm run dev 启动方式与验证步骤。
```

**验收标准**：
- [ ] `npm run dev` 启动，浏览器打开能登录（后端对应接口已通）
- [ ] 登录后进入布局页，菜单可切换，未登录访问 /dashboard 自动跳登录
- [ ] 代理生效（浏览器 Network 里 /api 请求 200）

---

> 🎨 **视觉规范必读**：所有前端页面（含段 5、6）必须遵循 [苹果风设计规范.md](./苹果风设计规范.md)——该规范提炼自 emilkowalski/skills 的 apple-design skill（字体层级 34/28/22/17/15/13、8pt 间距网格、圆角 6/10/14/999、柔和阴影、系统色板）。开发时把该规范文档一并提供给 AI。

### 段 6：前端核心页面（约 4 小时，可分 2 次完成）

**段目标**：实现实时监测大屏、历史曲线、设备管理、告警中心、阈值设置、用户管理 6 大页面。

**参考项目**：Opikadash（实时可视化）、aroyy007（历史分析 UI）、airpulse（仪表盘）

**AI 提示词**：

```
请为 nepsystem-web 实现以下页面（全部走 src/api 封装，Element-Plus + ECharts）：

【页面1 实时监测大屏 /dashboard】（重点页面，美观第一）
- 顶部：总览统计卡片（设备总数/在线数/今日上报数/未处理告警，调用 /api/stats/overview）
- 中部：设备列表卡片，每个设备显示最新各指标值 + 状态标签（在线/离线）+ 超标红色高亮（对比 /api/stats/quality 得分）
- 图表区：ECharts 折线图实时趋势（每 5 秒轮询 /api/data/latest，最近 20 个点），Gauge 仪表盘组件展示 PM25/TEMP 等关键指标（用 echarts gauge）
- 告警滚动条：最新未处理告警（轮询 /api/alerts/unhandled/count + 最新 5 条）
- 大屏深色主题（深蓝底 #0f1c3d 风格），整体要像"监控大屏"

【页面2 历史数据 /history】
- 筛选器：设备下拉、指标多选、时间范围（el-date-picker）
- ECharts 多指标折线图（调 /api/data/trend，interval=hour）
- 明细表格分页（调 /api/data/history）
- 导出按钮：调用接口返回 CSV（后端加 GET /api/data/export 生成 CSV 下载）

【页面3 设备管理 /devices】
- 表格分页 + 关键字/类型/状态筛选 + 新增/编辑对话框 + 删除确认
- 状态用 el-tag 彩色标签

【页面4 告警中心 /alerts】
- 表格：级别（预警黄色/报警红色 tag）、指标、数值、时间、状态、操作（处理）
- 筛选：级别/状态/设备；未处理数在标题处红点提示
- 处理按钮二次确认

【页面5 阈值设置 /thresholds】
- 表格展示设备级与全局阈值，新增/编辑对话框（warn/alarm 上下限），启用开关

【页面6 用户管理 /users】
- 表格 + 新增/编辑/删除/启用禁用（后端补 /api/users 标准 CRUD）

要求：每个页面组件化；表格统一用 el-table + el-pagination；加载态 loading；空数据显示 el-empty；完成后逐页给出验证步骤。
```

**验收标准**：
- [ ] 6 个页面均可操作且数据真实（来自模拟器）
- [ ] 大屏自动刷新、超标高亮、告警滚动正常
- [ ] 历史趋势图、CSV 导出正常
- [ ] 各 CRUD 页面增删改查生效

---

### 段 7：WebSocket 实时推送 + 联调测试（约 2 小时）

**段目标**：数据与告警实时推送（不靠轮询），全链路联调，补测试与演示数据。

**参考项目**：CJL-build（实时显示）、smart-env-monitor（实时推送）

**AI 提示词**：

```
完成以下收尾工作：

【WebSocket 实时推送】
1. 后端：spring-boot-starter-websocket 依赖；WebSocketConfig 注册 /ws/notify 端点；NotifyWebSocketHandler 管理 session 集合；
2. 数据上报成功后，广播 JSON：{type:"data", deviceCode, sensorCode, value, reportTime}；告警产生时广播 {type:"alert", alertId, level, message}；
3. 前端：新建 utils/ws.ts 封装 WebSocket 连接（token 校验、断线重连）；大屏页面订阅后实时更新 ECharts 与告警列表（替代轮询，保留兜底轮询 30 秒一次）；
4. 浏览器可打开两个窗口验证实时同步。

【联调与测试】
5. 用 curl 写一个 smoke-test.sh（Windows 用 smoke-test.bat）：启动后依次验证 /api/stats/overview、设备分页、上报一条数据、查最新数据、查告警分页；
6. 后端补 2 个核心单元测试：数据上报事务回滚测试（构造异常场景断言 alerts 不写入）、阈值判断测试（边界值 warn/alarm）；
7. 生成演示数据：往 monitor_data 灌 3 天历史数据（每 10 分钟一条，约 1300 条/设备，用 SQL 存储过程或循环插入），保证大屏/趋势图有图可看。
```

**验收标准**：
- [ ] 双窗口实时同步（数据与告警）
- [ ] smoke-test.bat 全绿
- [ ] 单元测试通过（mvn test）
- [ ] 历史数据充足，趋势图完整

---

### 段 8：打包部署与文档收尾（约 1.5 小时）

**段目标**：产出可交付的打包产物与完整文档（毕业设计友好）。

**参考项目**：MatoYing/GraduationProject（毕设文档组织）

**AI 提示词**：

```
完成项目收尾：

1. 后端打包：确认 pom.xml 的 skip 已删，执行 mvn clean package -DskipTests 生成可运行 jar；给出 java -jar 启动方式；
2. 前端打包：npm run build，产物 dist/ 说明如何用 nginx 托管（附 nginx.conf 示例，含 /api 反代到 8080）；
3. 项目根目录生成 README.md，包含：项目简介、功能清单、技术栈、系统架构图（ASCII）、目录结构、环境要求（JDK21/Maven/Node/MySQL）、快速启动步骤（1 建库 2 导数据 3 改配置 4 启动后端 5 启动前端）、默认账号密码、演示账号说明；
4. 生成 docs/ 目录：
   - docs/数据库设计.md（表结构说明 + ER 图 ASCII）
   - docs/接口文档.md（按模块列出全部接口：方法/路径/参数/返回示例）
   - docs/部署手册.md（Windows 本机部署 + 可选 Docker Compose：mysql + 后端 + 前端 nginx 三容器）
   - docs/毕业设计说明.md（选题背景、需求分析、系统设计、核心功能实现说明、测试报告、总结——可直接作为论文素材）
5. 若工作区尚未 git init，执行 git init 并生成 .gitignore（含 target/ node_modules/ dist/ .idea/ .vscode/），提交初始版本。
```

**验收标准**：
- [ ] `java -jar` 可独立运行后端
- [ ] README 按步骤操作可完整复现（新人 30 分钟跑通）
- [ ] 4 份文档齐全
- [ ] git 仓库已初始化并提交

---

## 3. 使用说明

1. **执行顺序**：段 1 → 段 8 严格顺序执行；段内任务可拆多次对话完成。
2. **每段开始**：把该段的"AI 提示词"整段粘贴给任意 AI 编程助手（本会话/其他工具均可），并附上"全局提示词"。
3. **每段结束**：逐条核对"验收标准"，未通过则让 AI 修复后再进入下一段。
4. **GitHub 复用**：对应段的"参考项目"先浏览一遍（或让 AI 检索其结构），把设计思想带入提示词中。
5. **遇到环境问题**（端口占用、依赖下载失败、密码错误等）：把完整报错给 AI，按"全局提示词"第 8 条处理。
6. **本会话可直接执行**：当前工作区已具备全部环境，可以直接让本助手按本流程逐段开发。

---

## 4. 预计总工时

| 段 | 内容 | 预计 |
|---|---|---|
| 1 | 基础设施修复与数据库设计 | 1h |
| 2 | 后端数据模型与通用封装 | 1.5h |
| 3 | 后端核心业务 API | 3h |
| 4 | 模拟数据采集器 + MQTT 预留 | 2h |
| 5 | 前端工程搭建 | 2h |
| 6 | 前端核心页面 | 4h |
| 7 | WebSocket + 联调测试 | 2h |
| 8 | 打包部署与文档 | 1.5h |
| **合计** | | **约 17 小时** |